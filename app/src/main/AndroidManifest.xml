<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.formula97.andorid.car_kei_bo"
    android:installLocation="auto"
    android:versionCode="4"
    android:versionName="0.7-p3">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name="net.formula97.andorid.car_kei_bo.TheApp"
        android:theme="@style/Theme.AppCompat">

        <activity android:name=".activity.CarListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddMyCarActivity" />
        <activity android:name=".ConfigActivity" />
        <activity android:name=".MileageListActivity" />
        <activity android:name=".FuelMileageAddActivity" />
        <activity android:name=".AboutLicenseActivity" />
        <activity android:name=".ShowStatsActivity" />
    </application>

</manifest>